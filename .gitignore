# Source / reference material — not part of the deliverable
*.pdf
law.txt
law.html
pages/
*_clean.pdf
*_repaired.pdf

# …but DO ship the sample PDFs served at /sample-*.pdf (test uploads)
!app/public/sample-courrier.pdf
!app/public/sample-response.pdf

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Local secrets (never commit)
.env
.env.local
*.pem
*.key

# Claude Code local cache
.claude/

# Database backups (contain real data — never commit)
backups/
*.dump
*.sql.gz
